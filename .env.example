# Translation API configuration
# Leave VITE_API_URL empty for same-origin deployment via nginx reverse proxy.
# Set to full URL (e.g., http://localhost:8000) for local development without nginx.
VITE_API_URL=

# API key for translator-api backend (public client key, rate-limited)
VITE_API_KEY=your-api-key-here
